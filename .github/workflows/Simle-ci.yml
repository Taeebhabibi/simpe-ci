on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: run file
        run: python3 test.py
